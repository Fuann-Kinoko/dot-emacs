;;; configs/packages/holo-layer.el -*- lexical-binding: t; -*-


;; (use-package! holo-layer
;;   :config
;;   (require 'holo-layer)
;;   (holo-layer-enable))

;; (setq holo-layer-enable-cursor-animation t)
;; (setq holo-layer-enable-window-border t)
;; (setq holo-layer-enable-indent-rainbow t)
